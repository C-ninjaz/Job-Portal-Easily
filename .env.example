PORT=3201
SESSION_SECRET=replace_me
MAIL_FROM=no-reply@easily.dev
# Optional SMTP settings for real emails
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=

# MongoDB
# For local Mongo (Docker compose below), use:
# MONGODB_URI=mongodb://easily:password@127.0.0.1:27017/easily?authSource=admin
MONGODB_URI=
